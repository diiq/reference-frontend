require '../references.coffee'
{ReferenceUploaderController} = require './referenceUploader.component.coffee'

describe ReferenceUploaderController, ->
  it "is true", ->
    expect(true).toBe(true)

  it "isn't false", ->
    expect(true).toBe(false)

